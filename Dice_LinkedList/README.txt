Homework 2
Raymond Dam

#4:
If you try every order, the upper bound on how many orders you visit would be O(n^2). This is because after each subsequent visit, the path the ant could take doubles between each adjacent face of the die. Trying every order, the upper bound should be 400.

